# Hugo Blog

A Hugo-powered blog deployed to GitHub Pages via GitHub Actions.

## Theme
Uses the `perfectfuckingblog` custom theme located in `themes/`.

## Deployment
The site is automatically deployed to GitHub Pages when pushing to the main branch via the GitHub Actions workflow in `.github/workflows/deploy.yml`.